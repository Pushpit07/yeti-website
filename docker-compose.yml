version: "3.9"
services:
  web:
    build: .
    image: yeti-website:latest
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 512M

